import styled from 'styled-components'

export const GalleryStyled = styled.div`
  .lightwidget-widget {
    display: grid;
    margin: 3rem auto;
    width: 80rem;
    border: none;
    height: 40rem;
  }
`
